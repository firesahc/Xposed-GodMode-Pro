// =========================================================================
// 根构建脚本 — Xposed-GodMode
// 包含模块: :app (Xposed模块+设置UI), :engine (规则引擎库), :libxservicemanager (系统服务注入库)
// AGP 8.4.0, compileSdk 35, Java 8
// =========================================================================
// 插件版本由 gradle/libs.versions.toml 版本目录统一管理，
// 不再需要 buildscript { dependencies { classpath ... } } 声明
// 仓库由 settings.gradle 的 dependencyResolutionManagement 集中管理

task clean(type: Delete) {
    delete rootProject.buildDir
}
